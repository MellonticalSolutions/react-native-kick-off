const INITIAL_STATE = {
  user: {
    name: 'Hashir Khan',
  },
};

const AppReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default AppReducer;
